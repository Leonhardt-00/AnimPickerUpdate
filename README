1.0.1 版本日志：
    1.修复了在当前picker有背景图片时，切换到没有背景图片的picker，会提示保存的Bug。
    2.兼容了python2，现在maya2022以下版本也可以使用Animpicker了。

1.0.2 版本日志：
    1.增加创建多重节点。
    2.排版能力增加了“水平等分”和“垂直等分”。
    3.修复了在当前picker下新增节点，切换到其他picker时不触发保存的提示的Bug。
    4.增加了关闭AnimPicker时保存picker顺序，在下次打开时会还原。
    5.修复了用除了鼠标左键之外单击切换picker时，不更换卡片的Bug。
    6.脚本节点增加变量名显示。

1.0.3 版本日志：
    1.增加模板功能，模板无法被修改，支持用户自定义。
    2.给标签页添加文件路径的提示。
    3.保存的文件成为序列化压缩数据，缩小体积和防止被篡改。
    5.修复当文件为new时切换tab不提示保存的问题。
    6.编辑菜单下新增了“添加模板节点”功能，支持用户自定义。
    7.新增检查更新，当前只支持2022以上的自动更新。
